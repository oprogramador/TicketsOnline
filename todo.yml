todo:
    backend:
        correct_working: [yes, 'app.php as well app_dev.php']
        email_confirmation: [yes, 'not only localhost']
        email_checking: no
        multilang: no
        pools: [yes, 'required and not']
        startup_script: yes
        translation: yes
        validation: yes
    code:
        delete_logs: no
        indent: [no, 'including curly style']
        removing_unused_code: no
        signature: [no, 'on each file']
    frontend:
        css: no
        dynamic_info: no
        client_side_validation: no
